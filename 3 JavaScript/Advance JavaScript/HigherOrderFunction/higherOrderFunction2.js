/* ************************* */

/* function x() {    // x is CallBack Function
    console.log("Namaste");
}

function y(x) {  // y is higher order function
    x();
} */



/* // Repetition of the code 

const radius = [3, 1, 2, 4];

const calculateArea = function (radius) {   // Calculate Area
  const output = [];
  for (let i = 0; i < radius.length; i++) {
    output.push(Math.PI * radius[i] * radius[i]);
  }
  return output;
};

console.log(calculateArea(radius));

const calculateCircumference = function () {   // Calculate Circumference
  const output = [];
  for (let i = 0; i < radius.length; i++) {
    output.push(2 * Math.PI * radius[i]);
  }

  return output;
};

console.log(calculateCircumference(radius));


const calculateDiameter = function () {   // Calculate Diameter
    const output = [];
    for (let i = 0; i < radius.length; i++) {
      output.push(2 * radius[i]);
    }
  
    return output;
  };
  
  console.log(calculateDiameter(radius)); */


